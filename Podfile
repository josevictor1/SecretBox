# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SecretBox' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SecretBox
  pod 'Alamofire', '~> 4.5'
  pod 'Moya', '~> 10.0'

  target 'SecretBoxTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SecretBoxUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
